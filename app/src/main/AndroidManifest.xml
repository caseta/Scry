<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.taylorcase.hearthstonescry"
  xmlns:android="http://schemas.android.com/apk/res/android">

  <uses-permission android:name="android.permission.INTERNET" />
  <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
  <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

  <application
    android:name=".ScryApplication"
    android:allowBackup="true"
    android:icon="@mipmap/ic_launcher"
    android:label="@string/app_name"
    android:roundIcon="@mipmap/ic_launcher_round"
    android:supportsRtl="true"
    android:theme="@style/AppTheme.Warlock">
    <activity android:name=".SplashActivity">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />
        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
    </activity>
    <activity android:name=".base.BaseActivity" />
    <activity android:name=".CardsActivity" />
    <activity android:name=".DetailedCardActivity" />
    <activity android:name=".search.SearchActivity" />
    <activity android:name=".selecthero.SelectHeroActivity" />
    <activity android:name=".savedcards.SavedCardsActivity" />
    <activity android:name=".selecthero.DetailedSelectHeroActivity" />
    <activity android:name=".filter.FilterActivity" />

    <meta-data
      android:name="preloaded_fonts"
      android:resource="@array/preloaded_fonts" />

    <meta-data
      android:name="firebase_crashlytics_collection_enabled"
      android:value="false" />
  </application>

</manifest>